import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex justify-center items-center'>
            <button className=' btn loading'>Developer Shimul   ........</button>
        </div>
    );
};

export default Loading;